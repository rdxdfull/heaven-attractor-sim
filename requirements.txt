# Core numerical computation
numpy>=1.24

# CSV handling and dataframes (used for summary/per-step logging)
pandas>=2.0

# Optional plotting (for CLI plots)
matplotlib>=3.7

# Dashboard support
streamlit>=1.30
plotly>=5.15
